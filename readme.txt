Para executar a aplicação no linux via terminal, execute os seguintes passos:

1. cd para o diretório em que requirements.txt está localizado;

2. Abra o prompt de comando (terminal);

    1.2.execute: pip install -r requirements.txt

    1.3.execute: python3 Problema\ do\ Carteiro.py
